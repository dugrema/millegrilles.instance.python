# Use this file to have certificates issued for services that are not part of the "middleware" configuration yaml.
# This file is also used for the initial docker image pull (downloads).

include:
  - ../coremodules/certissuer.yml
  - ../coremodules/nginx.yml
  - ../coremodules/core.yml
  - ../coremodules/maitredescles.yml
